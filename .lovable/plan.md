

## Branding Metadata Update

### Changes

**1. `index.html`** — Meta tag overhaul:
- Update `<title>` to "Terralux Landscape | Premium Lawn & Outdoor Services"
- Remove `twitter:site` meta tag entirely
- Remove Lovable opengraph image URLs
- Add OG tags: `og:title`, `og:description`, `og:url`, `og:type`, `og:image`
- Add Twitter tags: `twitter:card`, `twitter:title`, `twitter:description`, `twitter:image`
- Keep existing `meta description` and `meta author`

**2. `public/og-image.jpg`** — New file:
- AI-generated branded 1200×630 image using the uploaded property photo as base
- Overlay with Terralux logo, tagline, and URL

**3. No changes needed:**
- Favicon already uses the icon-only SVG and is correctly referenced

